# Core backend
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.2
bcrypt>=4.0

# HTTP client (Mock EHR connector) + env loading
httpx>=0.27
python-dotenv>=1.0

# FastAPI multipart/form-data uploads (voice endpoints)
python-multipart>=0.0.9

# Gemini speech (STT/TTS) — uses the same GEMINI_API_KEY as the chat LLM
google-genai>=1.0

# AI agent
langgraph>=0.2
langchain-core>=0.3
langchain-groq>=0.2
langchain-google-genai>=2.0

# Frontend
streamlit>=1.38

# Testing
# NOTE: this prototype's unit/integration tests use the standard library
# `unittest` runner (no pytest needed):  python -m unittest discover -s tests -v
pytest>=8.0

# Optional: PostgreSQL in deployment (SQLite used by default locally)
# psycopg[binary]>=3.1
